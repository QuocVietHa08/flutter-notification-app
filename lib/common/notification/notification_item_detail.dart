import 'package:flutter/material.dart';

class NotificationItemDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Notification Item Detail"),
      ),
      body: Center(
        child: Text("Notification Item Detail"),
      ),
    );
  }
}